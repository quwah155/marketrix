diff --git a/node_modules/mongodb/lib/utils.js b/node_modules/mongodb/lib/utils.js
index 7e392ea..416627f 100644
--- a/node_modules/mongodb/lib/utils.js
+++ b/node_modules/mongodb/lib/utils.js
@@ -985,7 +985,7 @@ async function request(uri, options = {}) {
             method: 'GET',
             timeout: 10000,
             json: true,
-            ...url.parse(uri),
+            ...(() => { const u = new URL(uri); return { protocol: u.protocol, hostname: u.hostname, port: u.port || undefined, path: u.pathname + u.search, auth: u.username && u.password ? `${u.username}:${u.password}` : undefined }; })(),
             ...options
         };
         const req = http.request(requestOptions, res => {
